# Partition table — ESP32-S3-N16R8 (16MB Flash)
# IoT Voice Command System
#
# Layout (total usable ≈ 15.5MB after bootloader @ 0x0):
#   nvs       :  16 KB  — WiFi/BT config, NVS key-value store
#   otadata   :   8 KB  — OTA slot bookkeeping
#   phy_init  :   4 KB  — RF calibration data
#   factory   :   3 MB  — Main firmware (app)
#   model     :   3 MB  — srmodels.bin (WakeNet + MultiNet for ESP-SR)
#   storage   :   ~7 MB — SPIFFS: voice vectors, device state, config
#
# Name,     Type,  SubType,  Offset,    Size,    Flags
nvs,        data,  nvs,      0x9000,    0x4000,
otadata,    data,  ota,      0xD000,    0x2000,
phy_init,   data,  phy,      0xF000,    0x1000,
factory,    app,   factory,  0x10000,   0x300000,
model,      data,  spiffs,   0x310000,  0x300000,
storage,    data,  spiffs,   0x610000,  0x5F0000,
